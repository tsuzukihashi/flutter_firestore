name: flutter_firestore_demo
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  firebase_core: "^1.0.1"
  cloud_firestore: "^1.0.1"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true